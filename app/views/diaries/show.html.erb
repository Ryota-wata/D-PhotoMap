<% content_for(:title, t('.title')) %>
<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <h1><%= t'.title' %></h1>
                <%= @diary.title %>
                <%= @diary.day %>
                <%= @diary.body %>
                <%= @diary.lottely_facility %>
                <%= @diary.lottely_time %>
                <%= @diary.lottely_result %>
        </div>
        
        <%= link_to "編集", edit_diary_path(@diary) %>
        <%= link_to ”日記一覧”, diaries_path %>
    </div>
</div>

         
