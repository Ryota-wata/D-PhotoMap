<% content_for(:title, t('.title')) %>
<div class="container">
    <div class="col-md-6 col-md-offset-3">
        <h1 class="m-5 text-center fw-light title"><%= t'.title' %></h1>
    </div>
</div>

<%= render 'form', { photo: @photo } %>